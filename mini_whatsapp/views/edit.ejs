<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Chat</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container py-4">
    <div class="card shadow p-4">
      <h1 class="mb-3"><i class="bi bi-pencil-square"></i> Edit Chat</h1>
      <p class="text-muted mb-4">This chat was sent from <b><%= chat.from %></b> to <b><%= chat.to %></b></p>

      <form action="/chat/<%= chat._id %>?_method=PUT" method="POST" class="d-flex flex-column gap-3">
        <textarea name="msg" rows="6" class="form-control" required><%= chat.msg %></textarea>
        <div class="d-flex gap-2">
          <button class="btn btn-warning">
            <i class="bi bi-save"></i> Update
          </button>
          <a href="/chats" class="btn btn-secondary">
            <i class="bi bi-arrow-left-circle"></i> Back
          </a>
        </div>
      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
